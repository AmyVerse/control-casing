import React from 'react';
import '../App.css'; // Make sure to create and style this CSS file

const Navbar = () => {
  return (
    <header className="navbar">
      <h1 className="a">Change casing of your text</h1>
    </header>
  );
};

export default Navbar;